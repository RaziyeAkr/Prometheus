## Core count

```promql
count by (instance) (node_cpu_seconds_total{mode="idle"}
```

### This query count cpu's cores.

### Labels Used
- instance
- mode

### Example Output

### Alert Condition (Optional).
